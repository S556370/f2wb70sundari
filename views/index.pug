extends layout

block content
  h1= title
  p An Intresting fact about me is, I can make any one as friend in very short span   #{title}
  - var btnType = 'info'
  - var btnSize = 'lg'

  button(type='button' class=`btn btn-${btnType} btn-${btnSize}`) Press Me
